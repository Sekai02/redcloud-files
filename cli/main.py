"""CLI entry point: parses arguments and dispatches commands to Controller."""

def repl_loop() -> None:
    """Start a REPL loop (stub)."""
    while True:
        input(">>> ")  # Placeholder for REPL input handling.

def main() -> None:
    """Parse CLI args and execute commands (stub)."""
    repl_loop()

if __name__ == "__main__":  # pragma: no cover
    main()
